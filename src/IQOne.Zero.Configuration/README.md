# IQOne.Zero.Configuration

Options bound and validated at startup, so a missing or malformed setting stops the
application with a message naming it instead of failing the first time that code path runs.

Part of [Zero](https://github.com/keremcanaktas/zero) by IQOne.
